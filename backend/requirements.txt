# Core service
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# Optional — PDF resume ingestion
# pdfplumber>=0.11

# Optional — production-grade dense retrieval (config.EMBEDDING_BACKEND="st").
# Without these the deterministic lexical-semantic fallback is used; the
# pipeline, demo, and tests run on the Python standard library alone.
# sentence-transformers>=2.7
# qdrant-client>=1.9
